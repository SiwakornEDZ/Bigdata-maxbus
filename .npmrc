# Disable native builds
node-gyp=false
# Disable optional dependencies
omit=optional
# Disable audit
audit=false
# Disable fund messages
fund=false
# Use legacy peer deps
legacy-peer-deps=true
# Force use of specific packages
public-hoist-pattern[]=*pg*
public-hoist-pattern[]=*libpq*
